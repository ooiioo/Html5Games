/**
 * Created by alexgan on 15-3-17.
 */
var GameConfig;
(function (GameConfig) {
    // 当前分数
    GameConfig.score = 0;
    // 历史最高分数
    GameConfig.topScore = 0;
})(GameConfig || (GameConfig = {}));
